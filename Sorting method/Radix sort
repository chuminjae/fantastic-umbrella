# include <bits/stdc++.h>
using namespace std;
int a[4500000];
int count(int a){
    int b = 1;

    while(a / 10 !=0){
        a = a/10;
        b++;
    }
    return b;
}
int count1(int a, int c){
    int x = pow(10, c - 1);
    a = a / x;
    return a % 10;
}

int main(){
    int n;
    queue<int>q[10];
    int b = 0;
    cin >> n;
    for(int i = 0; i < n; i++){
        cin >> a[i];
        b = max(b, a[i]);
    }
    int cnt = count(b);
    for(int j = 1; j <= cnt; j++){
        for(int i = 0; i < n; i++){
            q[count1(a[i], j)].push(a[i]);
            }
            int p = 0;
        for(int k = 0; k < 10; k ++){
            while(!q[k].empty()){
                a[p] = q[k].front();
                q[k].pop();
                p++;
            }
        }
        }
        for(int i = 0; i < n; i++){
            cout << a[i] << " ";
        }
}
